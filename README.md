# Java based Rest Clients Library
---
* Single library to use all java based Rest clients

---
# Important commands
* Create new child module. Replace `moduleName` with your desired module name
```maven
mvn archetype:generate -DgroupId=com.rcl -DartifactId=moduleName -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
```